'use client'

import { useEffect, useState } from 'react'

export function EnvChecker() {
  // Since we're using local storage now, we don't need to check for NextAuth environment variables
  // This component is now disabled for the local storage implementation
  return null
}
